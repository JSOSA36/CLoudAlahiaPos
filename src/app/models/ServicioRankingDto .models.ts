export interface ServicioRankingDto {
  nombreServicio: string;   // Nombre del servicio o producto
  veces: number;            // Cantidad de veces vendido
  totalFacturado: number;   // Monto total facturado por ese servicio
}
